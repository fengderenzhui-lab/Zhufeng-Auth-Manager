<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | 默认哈希驱动
    |--------------------------------------------------------------------------
    |
    | 使用 Argon2id（抗 GPU 爆破、内存密集），满足等保口令存储要求。
    | 旧 bcrypt 哈希在登录校验通过后由 AuthService 自动升级，无需人工干预。
    |
    */

    'driver' => 'argon2id',

    /*
    |--------------------------------------------------------------------------
    | Bcrypt 选项（兼容旧密码校验）
    |--------------------------------------------------------------------------
    */

    'bcrypt' => [
        'rounds' => env('BCRYPT_ROUNDS', 12),
    ],

    /*
    |--------------------------------------------------------------------------
    | Argon2 选项
    |--------------------------------------------------------------------------
    |
    | memory 65536 KiB（64MB）/ time 4 / threads 4 为 Laravel 生产推荐档，
    | 兼顾安全强度与 PHP-FPM 并发开销；生产环境可通过 ARGON2_MEMORY / ARGON2_THREADS / ARGON2_TIME 覆盖。
    |
    */

    'argon' => [
        'memory' => (int) env('ARGON2_MEMORY', 65536),
        'threads' => (int) env('ARGON2_THREADS', 4),
        'time' => (int) env('ARGON2_TIME', 4),
    ],

];
